---
name: womenhealth-app-development
description: "WomenHealth Android app — full architecture, implementation, and debugging session. 96 Kotlin files, modular MVVM+Clean Architecture, compiles successfully."
metadata: 
  node_type: memory
  type: project
  originSessionId: c419bcd5-e745-434c-a018-26c8061ae246
---

# WomenHealth App 开发会话记录 (2026-05-29 ~ 2026-05-30)

## 项目概述

为女性生理健康管理 Android App，从零设计架构到完整 96 文件 Demo 实现。

**Why:** 基于用户需求文档 `新app要求.txt` 和 `问题回应.txt`，构建以最终产品为目标的 Demo。
**How to apply:** 下次会话直接参考此文档恢复上下文，项目在 `D:\claudecode_workspace\WomenHealthApp\`。

## 用户信息

- Git: 见 git log（已脱敏）
- GitHub: `wiseliang/WomenHealthApp` (https://github.com/wiseliang/WomenHealthApp)
- 平台偏好: Android，本地存储优先
- 网络环境: 国内，需要使用阿里云/腾讯云镜像
- 设备: 使用真机 USB 调试（不依赖模拟器）

## 需求确认

基于 `新app要求.txt` 和 `问题回应.txt` 的最终设计决策:

| 需求 | 决定 |
|------|------|
| 平台 | Android (Kotlin + Jetpack Compose) |
| 后端 | 需要，但测试阶段本地存储优先 (Room) |
| 饮食评估 | AI识别 + 手动补录 + 食物数据库（后因ML Kit不可用，改为纯手动录入） |
| 健康联动 | 华为健康 HMS Health Kit（后因SDK不可用，改用Mock） |
| 激素校准 | 多维度可选填（BBT/情绪/皮肤/睡眠/宫颈黏液） |
| 数据来源 | 自行调研公开文献，界面标注引用 |
| 定位 | 以最终产品为目标的 Demo |
| minSdk | 26 (Android 8.0) |

## 技术栈

- Kotlin 2.1.0, Jetpack Compose BOM 2025.01.00
- Room 2.6.1, Hilt 2.52, Navigation Compose 2.8.5
- CameraX 1.4.1, ML Kit Food Detection 17.0.0 (已移除)
- HMS Health Kit 7.1.0.300 (已移除)
- WorkManager 2.10.0, Retrofit 2.11.0, Coil 2.7.0
- Gradle 8.9, AGP 8.7.2, KSP
- 测试: JUnit 5 + Turbine

## 项目结构 (13 个 Gradle 模块)

```
WomenHealthApp/
├── app/                            (8 kt)  Application, MainActivity, Theme, NavHost, DI
├── core/
│   ├── model/                      (11 kt) 纯 Kotlin 领域模型
│   ├── database/                   (23 kt) Room Entity/DAO/AppDatabase + DataInitializer
│   ├── network/                    (骨架)
│   ├── datastore/                  (骨架)
│   └── common/                     (1 kt)  DataExporter
├── feature/
│   ├── cycle/                      (14 kt) 经期追踪 + 预测算法 + 12 tests
│   ├── hormone/                    (10 kt) 激素评估 + 症状记录 + 10 tests
│   ├── recommendation/             (5 kt)  24条建议 + 20篇引用
│   ├── diet/                       (10 kt) CameraX 拍照 + 20种食物库 + 手动录入
│   ├── healthsync/                 (4 kt)  MockHealthManager (HMS SDK已移除)
│   ├── profile/                    (2 kt)  用户资料 + 华为连接UI + 数据导出
│   └── onboarding/                 (5 kt)  4步引导流程
└── gradle/                         libs.versions.toml
```

总计: **96 Kotlin + 2 JSON + 7 XML + 14 Gradle = ~127 文件**

## 6 阶段开发过程

### Phase 0: 基础框架
- 创建 Gradle 多模块项目、Version Catalog、Hilt DI、Navigation Compose 路由
- 10个 Room Entity + 11个 DAO + TypeConverter + AppDatabase
- 所有领域模型、Onboarding 引导流程、Feature 占位界面

### Phase 1: 经期追踪 (Cycle)
- **CyclePredictor**: 加权移动平均算法，≥3个周期开始预测，排卵日=下次经期-14天
- **PhaseCalculator**: 按周期天数计算4个相位
- **CalendarScreen**: 月历网格、经期日/预测日/排卵日着色、相位横幅、预测卡片
- **PeriodLogSheet**: Material3 DatePicker 选择起止日期
- **12个单元测试**: 0/1/2/3+周期、不规则周期、权重验证

### Phase 2: 激素评估 (Hormone)
- **HormoneEstimator**: 基于规则的症状评分法，雌激素/孕激素基线+修正量，可信度评分
- **HormoneDashboardScreen**: 双柱状图评估卡片、Canvas自绘趋势图、BBT体温图
- **SymptomLogSheet**: FilterChip选择 + BBT输入，全部可选填
- **10个单元测试**: 相位基线、黏液修正、BBT双相检测

### Phase 3: 个性化建议 (Recommendation)
- **24条建议**: 4个相位 × 多种分类 × 3种健身目标筛选
- **20篇引用文献**: 真实 PMID/DOI，以 JSON 存在 `core/database/src/main/assets/`
- **DataInitializer**: 首次启动自动写入 Room
- **RecommendationListScreen**: 相位横幅→分类分组卡片→展开详情→引用弹窗(含PubMed链接)

### Phase 4: 饮食记录 (Diet)
- **MealCaptureScreen**: CameraX PreviewView + ImageCapture
- **FoodDatabaseService**: 20种内置中国常见食物（米饭、鸡胸肉、饺子等），模糊匹配
- **MealConfirmationScreen**: 热量/宏量汇总→餐食类型→AI未识别时手动搜索+快捷添加
- **MealListScreen**: 每日热量卡片(总摄入/目标占比%)→按餐食类型分组→来源标签
- ⚠️ ML Kit Food Detection 不可用 → 改为拍照后直接手动录入

### Phase 5: 华为健康同步 (HealthSync)
- **HealthSyncManager**: 抽象接口（可用性检测、授权、步数/体重读取）
- **MockHealthManager**: Demo模式生成模拟数据
- **HealthSyncWorker**: WorkManager定时同步（后因 hilt-work 缺失而移除）
- **ProfileScreen**: 身体参数编辑+BMI、华为连接状态、步数/体重展示
- ⚠️ HMS SDK 不可用 → 移除依赖，仅保留Mock

### Phase 6: 打磨
- **DataExporter**: CSV导出（经期+症状+饮食），FileProvider + ShareSheet
- **App Icon**: 自适应图标（心形 + 排卵点）
- **Theme**: 支持 Dynamic Color + 深色模式
- **.gitignore**: Gradle/IDE/OS 文件

## 编译调试记录（重要）

以下是我们修复的所有编译错误，按顺序排列：

### 1. Gradle 下载速度慢
- **原因**: `services.gradle.org` 和 `dl.google.com` 在国内很慢
- **修复**: `settings.gradle.kts` 添加阿里云 maven 镜像；`gradle-wrapper.properties` 改用腾讯云镜像
- **全局方案**: `~/.gradle/init.gradle` 中配置阿里云 repos

### 2. `dependencyResolution` → `dependencyResolutionManagement`
- settings.gradle.kts 中方法名写错

### 3. `core:model` 缺少 `android-library` 插件
- `kotlin.android` 不提供 `namespace`，需加 `com.android.library`

### 4. `@Serializable` 不支持 `java.time.LocalDate`
- **原因**: kotlinx.serialization 无内置 LocalDate 序列化器
- **修复**: 从含 LocalDate 的 data class 移除 `@Serializable`，保留 enum 上的
- **影响文件**: PeriodRecord, CyclePrediction, DailySymptom, HormoneAssessment, HealthSyncRecord
- **注意**: enum 类型的 `@Serializable` 和 `import kotlinx.serialization.Serializable` 保留

### 5. `core:common` 缺少 `core:database` 依赖
- DataExporter 引用 DAO 类

### 6. ML Kit Food Detection 18.0.0 / 17.0.0 不存在
- 阿里云镜像没有 ML Kit → 移除依赖，删除 FoodDetectionService.kt
- CalorieAssessmentPipeline 简化为直接返回空结果（走手动录入）

### 7. HMS Health Kit 7.1.0.300 不存在
- 移除依赖，删除 HuaweiHealthManagerImpl.kt
- HealthSyncModule 直接使用 MockHealthManager

### 8. `DailySymptomDao.getRecentSymptoms` LIMIT 参数未使用
- 查询中写死 `LIMIT 60` 而非 `LIMIT :limit`

### 9. `Icons.AutoMirrored.Filled.ArrowBack` 不可用
- 换成 `Icons.Filled.Close`

### 10. `LocalDate.plusDays()` 需 Long 参数
- `plusDays(28)` → `plusDays(28L)`

### 11. CalendarScreen 缺少 `PeriodRecord`/`CyclePrediction` import
- 补充 `import com.health.model.PeriodRecord` 和 `CyclePrediction`

### 12. RecommendationListScreen cross-module smart cast
- `detailHtml` 跨模块不能智能转换 null-check → 用局部变量 `val html = ...`

### 13. `ProfileViewModel` 的 `HealthSyncRepository` 无法解析
- profile 模块缺少 `implementation(project(":feature:healthsync"))`

### 14. app 模块缺少 Room 和 kotlinx.serialization 依赖
- `DatabaseModule` 引用 `Room.databaseBuilder()` 需 `room-runtime`
- Screen.kt 的 `@Serializable` 需 `kotlinx-serialization-json`

### 15. PeriodLogSheet DatePicker 实验性 API
- `DatePickerDialog`/`rememberDatePickerState` 需 `@OptIn(ExperimentalMaterial3Api::class)`

### 16. HealthSyncWorker 依赖 `hilt-work` 缺失
- 删除 HealthSyncWorker.kt 及相关引用，Demo 阶段不需要后台同步

### 17. MealListScreen 缺少 `background` import
- 补充 `import androidx.compose.foundation.background`

## 当前状态

- ✅ 项目编译成功 (BUILD SUCCESSFUL)
- ✅ 22 个单元测试 (CyclePredictorTest 12个 + HormoneEstimatorTest 10个)
- ✅ 本地 Git 仓库（8 个提交，推送因网络不可用暂未同步到 GitHub）
- ⚠️ GitHub push 未完成（网络问题），代码仅本地
- 所有代码在 `D:\claudecode_workspace\WomenHealthApp\`

## 已知限制 / 后续工作

1. **ML Kit**: 需要 Google Play Services 环境才能使用 AI 食物识别
2. **HMS Health Kit**: 需要华为 AppGallery Connect 注册和真实华为设备
3. **后端服务**: 尚未实现，数据仅本地存储
4. **通知提醒**: 未实现经期预测提醒
5. **国际化**: 仅支持简体中文
6. **后端 API**: core:network 模块仅有骨架
7. **单元测试**: 仅覆盖核心算法，UI 测试缺失
8. **ProGuard**: 未配置代码混淆

## 下次会话快速启动

```bash
cd D:\claudecode_workspace\WomenHealthApp
# 在 Android Studio 中打开此目录，Sync 后即可运行
```

关键配置文件:
- `gradle/libs.versions.toml` — 所有依赖版本
- `settings.gradle.kts` — 模块声明 + 镜像仓库
- `gradle/wrapper/gradle-wrapper.properties` — Gradle 分发 URL (腾讯云镜像)
- `core/database/src/main/assets/` — 预置数据 (JSON)

## 相关文件

- 原始需求: `D:\claudecode_workspace\新app要求.txt`
- 用户回应: `D:\claudecode_workspace\问题回应.txt`
- 架构方案: `C:\Users\Administrator\.claude\plans\curious-snuggling-cat.md`
- 使用说明: `D:\claudecode_workspace\WomenHealthApp\使用说明.md`
- README: `D:\claudecode_workspace\WomenHealthApp\README.md`
